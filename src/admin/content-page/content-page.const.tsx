import { ROUTE_PARTS } from '../../shared/constants';

export const CONTENT_PAGE_PATH = {
	OVERVIEW: `/${ROUTE_PARTS.admin}/core/${ROUTE_PARTS.content}`,
	CREATE: `/${ROUTE_PARTS.admin}/core/${ROUTE_PARTS.content}/${ROUTE_PARTS.create}`,
};
