{
  "name": "tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:e2e:install-chrome": "playwright install chrome",
    "test:e2e": "npx playwright test",
    "test:e2e:trace": "npx playwright test --trace on",
    "test:e2e:ui": "dotenv -e ../.env.dev npx playwright test -- --ui",
    "test:e2e:debug": "cross-env PWDEBUG=console dotenv -e ../.env.dev npx playwright test --workers=1 --retries=0 --max-failures=1",
    "test:e2e:debug:t01": "cross-env PWDEBUG=console dotenv -e ../.env.dev npx playwright test tests/t01-inloggen-basisgebruiker.spec.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.4"
  }
}
